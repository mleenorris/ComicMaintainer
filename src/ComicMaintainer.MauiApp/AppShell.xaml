<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ComicMaintainer.MauiApp.Views"
       x:Class="ComicMaintainer.MauiApp.AppShell">

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage"
            Icon="home.png" />

        <ShellContent
            Title="Files"
            ContentTemplate="{DataTemplate views:FilesPage}"
            Route="FilesPage"
            Icon="folder.png" />

        <ShellContent
            Title="Settings"
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Route="SettingsPage"
            Icon="settings.png" />
    </TabBar>

</Shell>

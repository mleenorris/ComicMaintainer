# Comic directories
WATCHED_DIR=/path/to/comics
DUPLICATE_DIR=/path/to/duplicates
CACHE_DIR=./cache

# User and Group IDs (set to match your host user)
PUID=1000
PGID=1000

# Web interface port
WEB_PORT=5000

# PostgreSQL password (change this to a secure password)
POSTGRES_PASSWORD=changeme

# Number of Gunicorn workers (optional, default: 2)
# GUNICORN_WORKERS=2
